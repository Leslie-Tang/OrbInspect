# Figure 1: Roman-numbered panels

The panel labels are I. Mission inputs, II. Camera and LOS, III. HCW transfer
to SOOA graph, IV. Rollout ADP, and V. Replanning. The manuscript caption uses
the same identifiers. This changes only the five prefixes in the preceding
compact-typography figure; geometry, fonts, colors, mathematical notation,
images, scientific content, and all other drawing objects remain unchanged.

The matching draw.io file is the editable master, with mathematical typesetting
enabled. SVG/PDF/PNG exports use the same label content and geometry. Native
source/export parity is checked; live draw.io/MathJax rendering is not claimed.

The source and image credits are retained in
`../figure1_typography_refined_20260908/README.md`, and compact mathematical
definitions are in that directory's `NOTATION_NOTES.md`. The previous versions
remain available unchanged. `QA.json` records the scoped verification.
