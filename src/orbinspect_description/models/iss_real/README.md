# iss_real

Visual-only NASA International Space Station GLB asset for Gazebo Harmonic demos.

OrbInspect still uses the simplified station primitives for target generation,
visibility checks, and safety distance calculations. The real ISS mesh is used
only to improve scene fidelity.
