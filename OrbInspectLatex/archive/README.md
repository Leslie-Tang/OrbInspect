# Preserved history; not used by the active manuscript

`preorganization_20260908/` is the complete previous LaTeX tree: original paths,
templates, historical data, figures, scripts and compiled files are preserved.
Treat it as a snapshot, not as the current source or a current regeneration target.
Original scripts assumed the full repository layout at the time of creation.

`figure2_alternatives/` contains the earlier wide and taller single-column
draw.io packages for comparison. The active version is in
`figures/fig02_rollout_example/`, relative to the manuscript root.

Nothing in this folder is required to build the current paper or included in
the portable source ZIP. No original files were deleted. See
`docs/organization_manifest.json` for the preservation inventory.
