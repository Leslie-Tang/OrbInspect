\section{Results}
\label{sec:results}

\subsection{Offline Planning Performance}

Table~\ref{tab:offline-results} summarizes the proposed SOOA tour in the
high-coverage mesh experiment. The planner reached 98.33\% coverage over
180 surface targets, exceeding the 70\% success threshold and passing the 98\%
planning stop target. Coverage is reported together with cumulative
$\Delta v$, minimum clearance, peak requested input, and clipping ratio because
these quantities determine whether an inspection sequence is dynamically
plausible. These results establish the scalable full-graph comparison only;
the sensitivity and ablation studies in Table~\ref{tab:validation-matrix} test
which parts of this ranking persist under denser target sampling and perturbed
mission conditions. The selected SOOA sequence also passes the reported
passive-drift audit with a minimum margin of 1.509~m over a 300~s uncontrolled
HCW drift horizon.

\begin{table}[t]
  \centering
  \caption{Best feasible offline planning metrics from the high-coverage mesh
  experiment.}
  \label{tab:offline-results}
  \begin{tabular}{ll}
    \toprule
    Metric & Value \\
    \midrule
    Method & Proposed SOOA tour \\
    Dynamics model & CW/HCW \\
    Geometry model & NASA ISS glTF mesh \\
    Target count & 180 \\
    Viewpoint seeds & 357 \\
    Selected SOOAs & 21 \\
    Trajectory samples & 945 \\
    Mission duration & 1890.0~s \\
    Final coverage ratio & 0.9833 \\
    Inspectable coverage ratio & 0.9833 \\
    Coverage threshold & 0.7000 \\
    Coverage success & Yes \\
    Total planned $\Delta v$ & 21.5921~m/s \\
    $\Delta v$ per coverage & 21.9581~m/s \\
    Maximum speed & 0.8898~m/s \\
    Minimum clearance & 9.9107~m \\
    Passive audit horizon & 300.0~s \\
    Minimum passive audit margin & 1.5091~m \\
    Passive audit passed & Yes \\
    RMS transfer tracking error & 19.9695~m \\
    Peak requested input & 0.0388~m/s$^2$ \\
    Clipped step ratio & 0.0000 \\
    Planning time & 5.6254~s \\
    Feasible trajectory & Yes \\
    \bottomrule
  \end{tabular}
\end{table}

Table~\ref{tab:certified-results} reports a separate reduced-graph oracle
check. The exact dynamic program used eight SOOA nodes from a 16-seed mesh
instance, covered 13 visible target IDs, expanded 39 feasible graph states,
and returned certificate status ``optimal''. The certified sequence selected
two arcs, reached the required 25\% raw coverage, and remained within the
input and clearance limits. This is intentionally not a headline coverage
result; it verifies that the finite SOOA objective and implementation can be
exhausted on a small graph.

\begin{table}[t]
  \centering
  \caption{Reduced-SOOA-graph oracle check. The exact dynamic program
  minimizes HCW dynamic cost subject to feasible transitions, a four-arc
  budget, and a 25\% coverage requirement.}
  \label{tab:certified-results}
  \renewcommand{\arraystretch}{1.05}
  \setlength{\tabcolsep}{4.0pt}
  \begin{tabular}{lrr}
    \toprule
    Method & Coverage & Dynamic cost \\
    \midrule
    Exact DP oracle & 0.2500 & 26.7682 \\
    Proposed scalable & 0.2917 & 27.0924 \\
    Safe coverage greedy & 0.2917 & 30.3664 \\
    \bottomrule
  \end{tabular}
\end{table}

Figure~\ref{fig:mesh-and-trajectory} overlays the planned trajectory on the
same open NASA ISS glTF mesh model used for target sampling and replay
visualization~\cite{nasa_iss_gltf_model}. The figure is generated from the
benchmark data, so the displayed path and selected SOOA terminal poses share the same LVLH
coordinate frame as the mesh. The path is a stitched sequence of discrete
rest-to-rest HCW transfers, so it is dynamically valid but not drawn as a
single global spline.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/high_coverage/proposed_trajectory_3d.pdf}
  \caption{Proposed SOOA tour overlaid on the open NASA ISS glTF mesh model.
  The selected SOOA terminal poses are colored by cumulative coverage, and the
  trajectory is shown as the planned chaser path in LVLH coordinates.}
  \label{fig:mesh-and-trajectory}
\end{figure}

Figure~\ref{fig:coverage-comparison} reports the corresponding coverage
evolution for the deterministic mesh comparison. All curves are generated from
the archived high-coverage experiment and use the same candidate set,
visibility model, transfer constraints, and termination logic.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/high_coverage/coverage_comparison.pdf}
  \caption{Coverage evolution for the compared offline methods in the
  high-coverage mesh experiment.}
  \label{fig:coverage-comparison}
\end{figure}

Figure~\ref{fig:trajectory-comparison} compares the baseline trajectories as
method-wise small multiples. The proposed SOOA tour is shown
separately in Fig.~\ref{fig:mesh-and-trajectory}, where the selected SOOA
terminal poses and full mesh context are easier to inspect. The shared mesh silhouette, viewpoint,
and axis limits make baseline path geometry comparable without repeating the
proposed trajectory. Figure~\ref{fig:trajectory-tradeoff-summary} reports the
full comparison in terms of final coverage and cumulative $\Delta v$, and it
marks dynamically infeasible runs.
In this representation, the proposed tour is the lowest-energy feasible method
that reaches the high-coverage stop target in the reported base case. The
coverage-greedy and
nearest-viewpoint runs can reach similar coverage only by violating input or
clearance constraints, whereas the safe-coverage and random-safe baselines
remain feasible but require substantially more cumulative $\Delta v$. The
fuel-focused baseline uses low input effort but stops below the high-coverage
target.
The baseline trajectory panels are intentionally unsmoothed between selected
SOOAs. The visible corners therefore reflect terminal HCW transfer segments,
not a rendering artifact.

\begin{figure}[t]
  \centering
  \subfloat[CW-NBV]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_cw_nbv.pdf}}\hfill
  \subfloat[Coverage]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_coverage.pdf}}
  \vspace{-0.35em}
  \subfloat[Safe cov.]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_safe_coverage.pdf}}\hfill

  \subfloat[Nearest]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_nearest.pdf}}\hfill
  \subfloat[Fuel]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_fuel.pdf}}
  \subfloat[Random]{\includegraphics[width=0.32\linewidth]{figures/high_coverage/trajectory_method_random.pdf}}
  \caption{Baseline trajectory comparison over the same ISS mesh silhouette.
  The proposed SOOA tour is shown separately in Fig.~\ref{fig:mesh-and-trajectory}.}
  \label{fig:trajectory-comparison}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/high_coverage/trajectory_tradeoff_summary.pdf}
  \caption{Coverage-effort-feasibility summary for the full offline comparison.
  The dashed line in the summary panel marks the 98\% stop target. A star
  denotes the proposed method shown in Fig.~\ref{fig:mesh-and-trajectory};
  circles denote feasible baselines, and crosses denote methods that fail the
  input, speed, or clearance checks.}
  \label{fig:trajectory-tradeoff-summary}
\end{figure}

Table~\ref{tab:method-execution-results} reports execution and solver
quantities that are not shown in
Fig.~\ref{fig:trajectory-tradeoff-summary}. Rest-to-rest terminal transfers
remove the earlier fly-through artifact in which a myopic safe baseline could
terminate after one view. Safe-coverage-greedy now reaches the same final
coverage as the proposed tour, but it requires more than twice the cumulative
$\Delta v$. Coverage-greedy reaches the target coverage with fewer views, but
it violates input and clearance checks. Nearest-viewpoint selection also
reaches high coverage, but its clearance violation makes the resulting tour
dynamically infeasible.

\begin{table*}[t]
  \centering
  \caption{Complementary offline comparison quantities not encoded by the
  coverage-effort plot.}
  \label{tab:method-execution-results}
  \renewcommand{\arraystretch}{1.08}
  \setlength{\tabcolsep}{5.0pt}
  \begin{tabular}{lrrrrrr}
    \toprule
    Method & SOOAs & Duration (s) & Peak input (m/s$^2$) &
    Clipped steps (\%) & Planning time (s) & Feasible \\
    \midrule
    Proposed & 21 & 1890 & 0.0388 & 0.0 & 5.63 & Yes \\
    CW-NBV baseline & 19 & 1710 & 0.0581 & 0.0 & 24.40 & Yes \\
    Coverage greedy & 18 & 1620 & 0.0849 & 10.5 & 1.21 & No \\
    Safe coverage greedy & 20 & 1800 & 0.0581 & 0.0 & 0.97 & Yes \\
    Nearest NBV & 36 & 3240 & 0.0528 & 0.0 & 4.82 & No \\
    Fuel greedy & 36 & 3240 & 0.0368 & 0.0 & 4.68 & Yes \\
    Random safe & 34 & 3060 & 0.0588 & 0.0 & 6.50 & Yes \\
    \bottomrule
  \end{tabular}
\end{table*}

Figure~\ref{fig:validation-matrix-summary} summarizes the sensitivity and
ablation matrix. Increasing the target count from 180 to 500 preserves the
main ranking: the proposed tour remains feasible and reaches the 98\% stop
target with 21.38~m/s cumulative $\Delta v$, whereas safe-coverage-greedy
requires 47.08~m/s and coverage-greedy remains infeasible. The coarser
$N=1000$ proposed-only stress case remains feasible but reaches 76.5\%
coverage because the candidate stride is increased to keep mesh visibility
computation tractable; this identifies candidate-density coupling as a
remaining scalability issue rather than hiding it.

The initial-condition sweep over three safe starting states keeps the proposed
tour feasible with final coverage 98.33\%, cumulative $\Delta v$ between
20.19 and 23.09~m/s, and positive clearance. Candidate-density sweeps show
that reducing candidate count from 357 to 120 still reaches at least 97.2\%
coverage but reduces clearance to 0.73~m in the proposed tour. Transfer
duration is the clearest feasibility boundary: a 70~s segment requests
0.061~m/s$^2$, slightly above the 0.060~m/s$^2$ limit, and is therefore
audited as infeasible; 90~s and 120~s segments are feasible.

The ablation study provides direct evidence for the algorithmic components.
Removing transfer cost increases cumulative $\Delta v$ and peak input while
retaining coverage. Removing the input-feasibility check produces high
coverage but violates the input limit. Removing clearance filtering changes
the selected set and reduces final coverage to 90.0\% in this benchmark.
Unweighted coverage is nearly identical here because the sampled ISS targets
have similar effective weights, but it remains an explicit ablation because
area weighting becomes important for nonuniform meshes.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/high_coverage/validation_matrix_summary.pdf}
  \caption{Sensitivity and ablation results generated by the offline
  validation matrix. Circles denote feasible runs and crosses denote runs that
  fail the full feasibility audit. The dashed lines mark the 98\% stop target
  or the 0.060~m/s$^2$ input limit.}
  \label{fig:validation-matrix-summary}
\end{figure}

\subsection{Camera Geometry and Replay Verification}

The high-coverage experiment exports trajectory and camera-attitude streams
for every compared method. For the proposed tour, each stream contains 945
samples. The camera orientation data replay the planned path with the same
boresight alignment used in the visibility predicate. The trajectory replay
node uses these data to publish odometry, path markers, field-of-view markers,
and station mesh markers for RViz and Gazebo validation.

Figures~\ref{fig:camera-model-frusta} and~\ref{fig:camera-model-coverage}
make the camera model visible in two ways. The first reconstructs
representative field-of-view frusta from selected SOOA terminal poses and boresight
vectors. The second reports the incremental surface coverage contributed by
the selected SOOA sequence. Together, they show that saved attitudes
instantiate the same FOV geometry used by the visibility predicate and that
the selected inspection actions contribute to the final coverage.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/high_coverage/camera_model_frusta.pdf}
  \caption{Camera-model verification through reconstructed field-of-view
  geometry overlaid on the proposed SOOA tour. The frusta and
  boresight rays are reconstructed from saved camera poses for representative
  early, middle, and final selected SOOAs, verifying that the logged attitude
  stream instantiates the visibility model used by the planner and by ROS
  replay in the same LVLH frame as the ISS mesh.}
  \label{fig:camera-model-frusta}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/high_coverage/camera_model_coverage.pdf}
  \caption{Incremental surface coverage contributed by the selected SOOAs.
  Bars show per-arc coverage gain in the selected SOOA sequence, with
  the same representative early, middle, and final arcs highlighted as in
  Fig.~\ref{fig:camera-model-frusta}.}
  \label{fig:camera-model-coverage}
\end{figure}

The ROS execution run reconstructs the planned state, attitude, and camera
frame from the saved logs, broadcasts the same FOV geometry used by the
planner, and records trajectory, control, coverage, mission-event, safety, and
planner topics. This verifies that the visual model, attitude stream, and
geometric visibility logic remain consistent after integration.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.48\linewidth]{figures/gazebo_validation/trajectory_3d.png}
  \includegraphics[width=0.48\linewidth]{figures/gazebo_validation/control_effort_over_time.png}
  \caption{ROS replay log verification for the planned SOOA execution. Left:
  replayed chaser trajectory in the same LVLH frame used by the offline
  planner. Right: logged control effort over replay time. These panels verify
  that the saved trajectory and command streams are available to the ROS
  logging and visualization stack; orbital dynamics claims remain based on the
  HCW planner logs.}
  \label{fig:ros-camera-verification}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.48\linewidth]{figures/gazebo_validation/chaser_follow_view_preview.png}
  \includegraphics[width=0.48\linewidth]{figures/gazebo_validation/full_preview_360.png}
  \caption{Gazebo Harmonic validation frames. Left: chaser-follow camera view.
  Right: full ISS visual context with chaser trajectory replay. Quantitative
  coverage, safety, and control claims come from recorded planner logs, not
  Gazebo physics.}
  \label{fig:gazebo-validation}
\end{figure}
