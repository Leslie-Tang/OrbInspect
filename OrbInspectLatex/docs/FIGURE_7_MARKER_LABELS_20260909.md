# Figure 7: waypoint numbers inside circles

Reduced only the trajectory waypoint labels from 7 pt to 5.2 pt Arial Bold.
The original circular marker area remains 57 pt squared (7.55 pt diameter),
including the existing white outline. All twelve labels retain their original
positions and observation identities. The two-digit labels now fit inside
their circles. Camera badges and metadata retain their previous font sizes.

The figure still links the odd/even trajectory projections to twelve original
camera images in two rows of six. It remains an image-and-trajectory composite
on a 170 by 47 mm canvas. Axis scale, trajectory coordinates, mesh, colors,
camera pixels, layout and all experimental results are unchanged. PDF/SVG text
remains editable; the PNG uses the same 600 dpi export.

The previous artwork and generator are preserved in
`archive/figure7_marker_labels_20260909/`. Regeneration uses the same curated
twelve-view snapshot and Arial font files as the previous approved figure.
The source checks, font audit, camera-region pixel comparison and manuscript
review are recorded in `FIGURE_7_MARKER_LABELS_QA_20260909.json`.
